# 2026-05-13

## 实现文件读写功能
- 给 `terminal_chat.py` 添加了完整的文件读写能力
- **机制**: DeepSeek Function Calling（OpenAI 兼容 API）
- **两阶段模式**: Phase 1 无声解析工具指令 → Phase 2 流式展示答案
- **5个工具**: read_file, write_file, list_directory, get_working_directory, append_file
- **沙箱安全**: 默认限制在 `D:\Game-Agent\` 目录，防路径穿越
- **新命令**: sandbox, sandbox <path>, readonly, allowpath <path>
- **零新依赖**: 纯 Python + 已有 OpenAI SDK

## 技能模块拆分重构
- 从 `terminal_chat.py` 提取 Sandbox 沙箱类 → `memory_store/skills/sandbox.py`
- 提取文件操作工具集（定义+执行器）→ `memory_store/skills/file_tools.py`
- `terminal_chat.py` 通过 import 引用拆出的模块，代码减少约 40%
- 拆出的类/函数: `Sandbox`, `FileToolSet`, `might_need_file_ops`
