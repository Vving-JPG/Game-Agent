# 知识库 (Knowledge Base)

> 将需要 RAG 检索的文档放入此目录（支持 .txt 格式）。
> 系统会自动分块建立 TF-IDF 索引，查询时返回最相关片段。

## 使用方法

1. 将任意 .txt 文档放入此目录
2. 重启 Agent 或输入 `11` / `rag` 命令手动重载
3. 聊天时，Agent 会自动检索相关文档片段

## 示例

将项目文档、API 参考、技术手册等放入此目录，
Agent 即可在回答时引用这些文档。

---

> 此目录下的文件会被自动索引，请不要放入敏感信息。
